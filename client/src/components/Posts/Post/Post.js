import React from "react";
import useStyles from "./styles";

const Post = () => {

    const {
        media,
        border,
        fullHeightCard,
        card,
        overlay,
        overlay2,
        grid,
        details,
        title,
        cardActions
    } = useStyles();
    return (
        <div>
            <h3>Post</h3>
        </div>
    );
};

export default Post;
